# Challenge

method is something that belong to an object orclass.It is also defined within a class defination.
While function is something that can be defined anywhere.A method can only be used to manipulate something related to it a class or its object.

# assignment
```
function funct1(a){
	console.log(a);
} 
```
or we can also write like this.
```
function funct2(a){
	return a;
}
```
here is the mixed parameters
```
function funct3( a =2 , b){
	console.log(a+b);
}
```

